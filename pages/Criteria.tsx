import React from 'react';

// This is a placeholder component to fix a potential build error.
// The file was empty, which can cause issues with some bundlers.
const Criteria: React.FC = () => {
  return null; // This component renders nothing.
};

export default Criteria;
